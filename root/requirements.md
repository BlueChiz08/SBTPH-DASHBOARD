## Packages
recharts | For data visualization charts (Bar, Line, Pie, Area)
date-fns | For robust date formatting and manipulation
framer-motion | For smooth page transitions and micro-interactions
jspdf | For exporting reports to PDF
html2canvas | Required for capturing DOM elements for PDF export
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind CSS classes without conflicts

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
